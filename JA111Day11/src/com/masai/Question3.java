package com.masai;

public class Question3 {

	/* >
	 * Abstract class can have abstract and non abstract methods,
	 * but Interface can have only abstract methods.
	 * >
	 * Interface supports multiple implementations,
	 * but abstract class support only single inheritance.
	 * >
	 * Abstract class uses extend, Interface uses implements keyword.
	 * >
	 * Abstract class used to achieve partial abstraction,
	 * Interface used to achieve complete abstraction.
	 * >
	 * Interface can have only final and static variables inside other than abstract methods,
	 * Abstract class can have final,non final,static and non static variables.
	 * 
	 * 
	 */
}
